{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist/public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/handler"
    }
  ],
  "functions": {
    "api/handler.js": {
      "maxDuration": 60
    }
  }
}
